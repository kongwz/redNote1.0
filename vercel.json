{
  "framework": "vite",
  "buildCommand": "npm run build",
  "installCommand": "rm -rf package-lock.json yarn.lock pnpm-lock.yaml node_modules && npm install --no-package-lock --legacy-peer-deps",
  "devCommand": "npm run dev",
  "rewrites": [
    {
      "source": "/api/(.*)",
      "destination": "/api/$1"
    },
    {
      "source": "/(.*)",
      "destination": "/index.html"
    }
  ]
}